To build the project, run ./gradlew build
while in  the project folder
The executable will be in ./build/libs

To run the project, run:
java -jar ./WeatherApp

Note: a prebuilt jar file is included in this repository
